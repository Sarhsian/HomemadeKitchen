@model CasualShop.DAL.Entities.Dishes

@{
    ViewData["Title"] = "Редагувати";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>Редагувати</h1>

<h4>Страви</h4>
<hr />
<div class="row">
    <div class="col-md-4">        
            <form asp-action="Edit" enctype="multipart/form-data">
                <fieldset>
                    <legend>Страви</legend>

                    @Html.HiddenFor(model => model.Id)

                    <p>
                        Страви <br />
                        @Html.EditorFor(model => model.Name)
                    </p>

                    <p>
                        Ціна <br />
                        @Html.EditorFor(model => model.Price)
                    </p>

                    <p>
                        Опис  <br />
                        @Html.EditorFor(model => model.Description)
                    </p>
                    <p>
                        Категорії <br />
                        @Html.DropDownListFor(model => model.CategoryId, ViewBag.Brands as SelectList)
                    </p>
                    @*<p>
                        Tags <br />
                        @Html.DropDownListFor(model => model.TagId, ViewBag.Tags as SelectList)
                    </p>*@
                    <p>
                        <div class="form-group">
                            <label >Назва фото</label>
                            <input asp-for="Image.Title" accept="image/*" />
                            <span asp-validation-for="Image.Title" class="text-danger"></span>
                        </div>
                        <div class="form-group">
                            <label >Назва файлу фото</label>
                            <input asp-for="Image.ImageFile" accept="image/*" />
                            <span asp-validation-for="Image.ImageFile" class="text-danger"></span>
                        </div>
                        @if (Model.Image != null)
                        {
                            <img src="@("~/Image/" + @Model.Image.ImageName)" asp-append-version="true" />
                            <input name="imgId" type="hidden" value="@Model.ImageId" />
                        }
                        else
                        {
                            <div>
                                <label>No Image</label>
                            </div>
                        }
                    </p>
                    <p>
                        <input type="submit" value="Зберегти" />
                    </p>
                </fieldset>
                </form>               
        </div>
</div>

<div>
    <a asp-action="Index">Повернутись до списку</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
